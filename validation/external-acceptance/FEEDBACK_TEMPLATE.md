# External acceptance feedback (manually redacted)

Copy into an issue only after reviewing every field. Do not attach actual
figures, datasets, screenshots, full logs, local paths or the raw local report.
Replace a failing workflow with a minimal synthetic example if disclosure is
appropriate; otherwise describe the supported/unsupported feature generically.

- Tested public commit/tag:
- MATLAB release and update (actual run, not planned):
- OS family/version (no hostname or user identity):
- LuaLaTeX / TeX distribution version, or unavailable:
- Neutral case ID:
- Outcome: PASS / PASS_WITH_VISUAL_DIFFERENCE / UNSUPPORTED_EXPECTED /
  FAIL_PRODUCT / FAIL_ENVIRONMENT / NEEDS_REVIEW
- Export succeeded / compilation succeeded:
- Scientific/visual comparison (generic, no research details):
- Diagnostic codes (redact raw messages):
- Approximate analysis / export / compilation seconds:
- Profile / width / image backend:
- Expected vs observed behavior:
- Impact: blocking scientific correctness / usability / cosmetic / environment:
- Small synthetic reproduction, if safe:
- Short redacted note:

Confirm before sharing: no source values, real figure names, research topics,
identities, machine paths, licenses, private repositories or sensitive images.
