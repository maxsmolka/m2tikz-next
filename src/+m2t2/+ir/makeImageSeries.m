function node = makeImageSeries()
%MAKEIMAGESERIES Construct a scalar matrix/image IR node.
    node = struct( ...
        'kind', 'm2t2.image', ...
        'id', 'series-1', ...
        'displayName', m2t2.ir.makeText(), ...
        'visible', true, ...
        'x', zeros(1, 0), ...
        'y', zeros(1, 0), ...
        'cdata', zeros(0, 0), ...
        'mapping', 'scaled', ...
        'interpolation', 'nearest');
end
